# Dados locais da caixa de e-mail (nao versionar)
# Usar saida/* (e nao saida/) para que a excecao abaixo funcione:
# o git nao reinclui arquivos se a propria pasta estiver ignorada.
saida/*
__pycache__/
*.py[cod]

# Exceto o JSON agregado usado pelo painel online
!saida/dashboard_data.json
